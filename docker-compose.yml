version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: gr-flight-app
    restart: always
    ports:
      - "${API_PORT:-4001}:${API_PORT:-4001}"
    env_file:
      - .env
    environment:
      - DB_HOST=host.docker.internal


